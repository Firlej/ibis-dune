# Paid Dune API key with Trino access. Required for success-path @pytest.mark.integration.
DUNE_API_KEY=
# Free-tier key (no Trino). Live tests assert paid-only DuneQueryError. Skip if unset.
DUNE_API_KEY_FREE=
